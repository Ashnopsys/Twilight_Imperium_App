<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.twilightimperiumiv">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.NoActionBar">
        <activity
            android:name=".CreateRace"
            android:label="@string/title_activity_create_race"
            android:theme="@style/NoActionBar"></activity>
        <activity
            android:name=".ListOfRaces"
            android:label="@string/title_activity_list_of_races"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".RaceInfo"
            android:label="@string/title_activity_race_info"
            android:theme="@style/NoActionBar" />
        <activity android:name=".MapDraw" />
        <activity
            android:name=".RandomRace"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.MaterialComponents.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>